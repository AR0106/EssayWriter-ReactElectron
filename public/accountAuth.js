import { getAuth, signInWithPopup, GoogleAuthProvider, EmailAuthProvider } from "firebase/auth";
import firebaseui from "firebaseui";

export default function authenticate(service) {
    var ui = new firebaseui.auth.AuthUI()
}